# ASR — same engine as ASR tab
mlx-qwen3-asr[serve]

# Web framework + ASGI
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Zero-copy PCM handling
numpy>=1.26.0

# Async HTTP client for OpenAI-compatible LLM streaming (flash-moe)
httpx>=0.27.0

# Postgres (optional: mind pulse persistence, memory spine)
asyncpg>=0.29.0

# TTS — Kokoro-82M via kokoro-onnx (ONNX runtime, no spacy needed, Python 3.14 compatible)
kokoro-onnx>=0.4.0

# Soundfile for encoding synthesized audio to WAV/PCM bytes
soundfile>=0.12.1
